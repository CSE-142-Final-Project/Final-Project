package com.csefinalproject.github.multiplayer.util;

/**
 * I think this is used to distinguish whether were launching as the server or the client
 */
public enum ProgramType {
	/**
	 * This indicates that we are going to run as a server
	 */
	SERVER,
	/**
	 * This indicates that we are going to run as a client
	 */
	CLIENT
}
