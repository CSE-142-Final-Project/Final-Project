package com.csefinalproject.github.multiplayer.util;

/**
 * I think this is used to distinguish whether or not were launching as the server or the client
 */
public enum ProgramType {
	SERVER,
	CLIENT
}
